{
  "name": "n8nloc",
  "version": "1.0.0",
  "scripts": {
    "start": "echo 'Starting n8n...' && n8n start",
    "build": "echo 'Installing dependencies...' && npm install",
    "debug": "node --version && npm --version && npx n8n --version"
  },
  "dependencies": {
    "n8n": "^1.112.6"
  },
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=9.0.0"
  }
}
